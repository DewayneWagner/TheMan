<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="TheManXS.ViewModel"
             mc:Ignorable="d"
             x:Class="TheManXS.View.DeveloperView">
    
    <ContentPage.Content>
        <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">            
            <Button Style="{StaticResource ButtonStandard}" Text="Clusters" Command="{Binding Cluster}"/>
            <Button Style="{StaticResource ButtonStandard}" Text="Settings" Command="{Binding Settings}"/>
            <Button Style="{StaticResource ButtonStandard}" Text="Database Delete" Command="{Binding DataBaseDelete}"/>
        </StackLayout>    
    </ContentPage.Content>
</ContentPage>